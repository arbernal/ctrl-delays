{% extends 'base.html.twig' %}

{% block body %}
    <h1>Roles</h1>
    <ul class="nav nav-pills">
  <li class="active"> <a href="{{ path('roles_new') }}" href="#">Agregar</a></li>
  <li><a href="#">Ver</a></li>
  <li><a href="#">Editar</a></li>
  <li><a href="#">Eliminar</a></li>
</ul>

    <table class = "table">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Estatus</th>
                <th>Accion</th>
            </tr>
        </thead>
        <tbody>
        {% for role in roles %}
            <tr>
                <td><a href="{{ path('roles_show', { 'id': role.idroles }) }}">{{ role.idroles }}</a></td>
                <td>{{ role.nombre }}</td>
                <td>{{ role.idesta }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('roles_show', { 'id': role.idroles }) }}">
                             <input type="submit" value="Ver"></a>
                        </li>
                        <li>
                            <a href="{{ path('roles_edit', { 'id': role.idroles }) }}">
                             <input type="submit" value="Editar"></a>
                        </li>
                        <li>
                         <input type="submit" value="Eliminar"></a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    
{% endblock %}
