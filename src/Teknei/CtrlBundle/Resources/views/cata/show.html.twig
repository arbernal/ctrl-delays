{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cata</h1>

    <table>
        <tbody>
            <tr>
                <th>Idcata</th>
                <td>{{ catum.idcata }}</td>
            </tr>
            <tr>
                <th>Desccort</th>
                <td>{{ catum.descCort }}</td>
            </tr>
            <tr>
                <th>Desccomp</th>
                <td>{{ catum.descComp }}</td>
            </tr>
            <tr>
                <th>Idesta</th>
                <td>{{ catum.idesta }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cata_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cata_edit', { 'id': catum.idcata }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
