{% extends 'base.html.twig' %}

{% block body %}
    <h1>RecaDesc</h1>

    <table>
        <tbody>
            <tr>
                <th>Idrecadesc</th>
                <td>{{ recaDesc.idrecaDesc }}</td>
            </tr>
            <tr>
                <th>Porce</th>
                <td>{{ recaDesc.porce }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recadesc_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('recadesc_edit', { 'id': recaDesc.idrecaDesc }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
