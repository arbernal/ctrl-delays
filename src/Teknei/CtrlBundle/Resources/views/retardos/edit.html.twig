{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nueva Junta</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
       <input type="submit" value="Editar" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('retardos_index') }}">	Cancelar</a>
        </li>
        </ul>
{% endblock %}
