{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo usuario</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('usuario_index') }}">Cancelar</a>
        </li>
    </ul>
{% endblock %}
